describe('Check initial setup', function() {
  it('confirms that Cypress is installed and passes a test', function() {
    expect(true).to.equal(true)
  })

  it('confirms that Cypress is installed and fails a test', function() {
    expect(true).to.equal(false)
  })
})
